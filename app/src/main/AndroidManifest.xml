<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.nayan.me.preventsuperbug">

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ArticleDetails"></activity>
        <activity android:name=".AntibioticDetails" />
        <activity android:name=".DoctorArticleList" />
        <activity android:name=".About" />
        <activity android:name=".Superbug" />
        <activity android:name=".Mentorship" />
        <activity android:name=".Article" />
        <activity android:name=".Complain" />
        <activity android:name=".Antibiotic" />
        <activity android:name=".Dashboard">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>